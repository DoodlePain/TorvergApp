import { StyleSheet } from "react-native";

export default StyleSheet.create({
  Background: {
    backgroundColor: "black",
    width: "100%",
    height: "100%",
    position: "absolute",
    top: "0%",
    left: 0
  }
});
